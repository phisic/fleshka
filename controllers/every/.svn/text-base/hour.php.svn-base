<?php
class zzRoot extends zzSite {
    protected $rootPath = __FILE__;

    function onLoad(){
/*        $catalog = zzNew('Market_Db_Flashrf_Catalog')->loadCatalogFilters(
            zzNew('zzSql_Filter_Group')->setGroup('id')->setIndex('feature_id')
        );

        foreach ($catalog as $id => &$c){
            $t = zzNew('Market_Db_Goods_GoodsGroup')->loadGoodsByFilter(
                 array('filter' => array_keys($c))
            );

            $new = array();
            foreach ($t as $cnt){
                $new[$cnt['goodsgroup_id']] = true;
            }

            $c = array(
                'id' => $id,
                'count' => count($new)
            );
        }unset($c);

        $catalog = zzNew('Market_Db_Goods_Catalog')->setCatalogCount(
            array('counts' => $catalog)
        );*/
    }
}
